var str = "";

function First_capital_letter(str) 
{
    str = str.split(" ");

    for (var i = 0, x = str.length; i < x; i++) {
        str[i] = str[i][0].toUpperCase() + str[i].substr(1);
    }
    document.getElementById("demo").innerHTML = str.join(" ");
}
